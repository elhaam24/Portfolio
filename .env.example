# Public URL only. This value is safe to expose in a Vite/Amplify build.
# Set it to the ContactApiUrl output after deploying backend/template.yaml.
VITE_CONTACT_API_URL=https://your-api-id.execute-api.your-region.amazonaws.com/contact
